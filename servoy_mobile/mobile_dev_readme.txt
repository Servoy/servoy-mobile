Testing/develop mobile client

make a solution and change the type to Mobile in the solution properties

create a form with some simple elements.

Then launch "Run mobile cient", this will compile the TiNG and creates a WAR that is then deployed at [install]\application_server\server\webapps\[solutionname] (war,dir)

in this dir there is a mobileclient directory which is the result of the mobile build

